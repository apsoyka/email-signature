{
    "name": "email-signature",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "html-minifier --collapse-whitespace --collapse-inline-tag-whitespace --minify-css true --minify-urls true --remove-comments --remove-optional-tags --remove-tag-whitespace --input-dir src --output-dir build"
    },
    "dependencies": {
        "html-minifier": "^4.0.0"
    }
}
